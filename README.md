# geadm

Read-only troubleshooting / debug / stats CLI for Google Gemini Enterprise
(Discovery Engine / Agentspace, `discoveryengine.googleapis.com`).

Documentation is completed once the command surface is built.
